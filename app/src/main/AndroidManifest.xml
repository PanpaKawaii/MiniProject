<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/duck1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/duck1"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniProject">
        <activity
            android:name=".MenuActivity"
            android:exported="true"><!-- set để test popup android:name=".subPopup"  -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignInActivity">
        </activity>
        <activity
            android:name=".SignUpActivity">
        </activity>
        <activity
            android:name=".RacingActivity">
        </activity>
        <activity
            android:name=".MoneyActivity">
        </activity>
        <activity
            android:name=".MainActivity">
        </activity>
        <activity
            android:name=".subPopup">
        </activity>
    </application>

</manifest>